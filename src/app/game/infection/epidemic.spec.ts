import { describe, it, expect } from "vitest";

describe("epidemic", () => {
  it("removes all supply cubes at bottom location", () => {
    expect(true).toEqual(true);

    // TODO make it easy in the game factory to create player city and infection cards
    //      based ont he cords already existing

    // const game = gameFactory.build(undefined, {
    //   transient: { locationNames: [LocationNames.SAN_FRANCISCO, LocationNames.CHICAGO] },
    // });
    // const gameLog: GameLog = vi.fn();
    //
    // const result = epidemic(game, gameLog);
    //
    // expect(result.cubesRemoved).toEqual(4);
  });
});
