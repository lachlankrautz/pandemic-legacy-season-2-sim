import { describe, it, expect } from "vitest";

describe("use player cards", () => {
  it("discards when done", () => {
    // TODO Setup a game and use some cards from a player.
    //      Then get stuck figuring out how to model the player hand
    //      and still be able to easily serialize etc
    expect(true).toEqual(true);
  });
});
